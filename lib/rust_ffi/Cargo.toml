[package]
name = "hello"
version = "0.1.0"
build = "build.rs"

[lib]
crate-type = ["cdylib"]
# If you only wanted dynamic library, you'd use only "cdylib".

[dependencies]
libc = "0.2.2"

[build-dependencies]
cbindgen = "0.29.0"
